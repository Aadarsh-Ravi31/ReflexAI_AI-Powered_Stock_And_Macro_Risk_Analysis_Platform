# Backend API base URL used by the frontend at build time.
# Local dev: leave unset (defaults to http://127.0.0.1:8000).
# Production (Vercel): set this to your Cloud Run service URL, e.g.
VITE_API_BASE=https://your-cloud-run-url.run.app
